---
- name: Stop and remove containers
  containers.podman.podman_container:
    name: "{{ item }}"
    state: absent
  loop:
    - "{{ docmost_app_name }}"
    - "{{ docmost_app_name }}-redis"
  failed_when: false

- name: Remove volumes
  containers.podman.podman_volume:
    name: "{{ item }}"
    state: absent
  loop:
    - "{{ docmost_app_name }}-data"
    - "{{ docmost_app_name }}-redis"
  failed_when: false

- name: Remove network
  containers.podman.podman_network:
    name: "{{ docmost_app_name }}"
    state: absent
    force: true
  failed_when: false
