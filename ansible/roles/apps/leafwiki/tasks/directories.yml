---
- name: Set ownership of mount point for pod user
  become: true
  ansible.builtin.file:
    state: directory
    path: "{{ leafwiki_data_dir }}"
    owner: "{{ leafwiki_service_user }}"
    group: "{{ leafwiki_service_user }}"
    mode: "0750"
    recurse: true
