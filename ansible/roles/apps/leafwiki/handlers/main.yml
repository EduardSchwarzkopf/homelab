---
- name: Restart service
  become: true
  become_user: "{{ leafwiki_service_user }}"
  ansible.builtin.systemd_service:
    name: "{{ leafwiki_app_name }}.service"
    daemon_reload: true
    state: restarted
    scope: user
  listen: Restart service
